/**
 * Main barrel export for all API services
 * Provides centralized access to auth, complaint, and user management services
 * 
 * Usage:
 *   import { authService, complaintService, userManagementService } from '@/functions/services';
 */
export * from './auth';
export * from './complaints';
export * from './users';
